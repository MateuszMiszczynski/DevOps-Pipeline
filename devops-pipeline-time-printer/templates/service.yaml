apiVersion: v1
kind: Service
metadata:
  name: {{ include "DevOps-Pipeline-time-printer.fullname" . }}
  labels:
    {{- include "DevOps-Pipeline-time-printer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "DevOps-Pipeline-time-printer.selectorLabels" . | nindent 4 }}
